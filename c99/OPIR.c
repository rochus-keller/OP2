// OPIR.c
// this file was generated by AoCodeNavigator 0.6.12 on Mi. März 4 17:25:17 2026

#include "OPIR.h"
#include <gc.h>
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <assert.h>
#include <math.h>
#include <ctype.h>
#include <stddef.h>

typedef struct $Class { struct $Class* super; } $Class;
static int $isinst(void* super, void* sub) {
    $Class* cls = ($Class*)sub;
    while(cls) {
        if( cls == super ) return 1;
        cls = cls->super;
    }
    return 0;
}
static MIC$DA* $toda(void** ptr) {
    return (MIC$DA*)((char*)ptr - offsetof(MIC$DA, $));
}
static void** $allocda(uint32_t count, int elemSize) {
    MIC$DA* ptr = (MIC$DA*)GC_MALLOC(sizeof(MIC$DA) + count * elemSize);
    if( ptr == 0 ) return 0;
    ptr->$1 = count;
    return ptr->$;
}

void OPIR$Instruction$init$(OPIR$Instruction* obj, unsigned int n) {
    int i;
    for( i = 0; i < n; i++ ) {
        obj[i].class$ = &OPIR$Instruction$class$;
    }
}

struct OPIR$Instruction$Class$ OPIR$Instruction$class$ = { 
    0,
};

void OPIR$_$1$init$(OPIR$_$1* obj, unsigned int n) {
    int i;
    for( i = 0; i < n; i++ ) {
        obj[i].class$ = &OPIR$_$1$class$;
    }
}

struct OPIR$_$1$Class$ OPIR$_$1$class$ = { 
    0,
};

/* public */ OPIR$Instruction* OPIR$Instr = NULL;

/* public */ int32_t OPIR$mapSize = 0;

/* public */ OPIR$_$1* OPIR$map = NULL;

void OPIR$BEGIN$() {
    OPIR$Instr = (OPIR$Instruction*)$allocda(500, sizeof(OPIR$Instruction));
}

static int OPIR$initialized = 0;
void OPIR$init$(void) {
    if( OPIR$initialized ) return;
    OPIR$initialized = 1;
    OPT$init$();
    OPM$init$();
    OPIR$BEGIN$();
}

