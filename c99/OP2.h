#ifndef __OP2_INCLUDED__
#define __OP2_INCLUDED__

// OP2.h
// this file was generated by AoCodeNavigator 0.6.10 on So. Feb. 22 22:43:43 2026

#include <stdint.h>
#include "OPP.h"
#include "OPB.h"
#include "OPV.h"
#include "OPT.h"
#include "OPS.h"
#include "OPC.h"
#include "OPL.h"
#include "OPO.h"
#include "OPM.h"
#include "Out.h"
#include "Texts.h"
#include "Oberon.h"

#define ASH$(x, n) (((n) >= 0) ? ((x) << ((n) >= 0 ? (n) : 0)) : ((x) >> ((n) < 0  ? -(n) : 0)))
#define MOD$(x, y) (((x) % (y) < 0) ? ((x) % (y) + (y)) : ((x) % (y)))
#define DIV$(x, y) ((((x) % (y) != 0) && (((x) < 0) != ((y) < 0))) ? ((x) / (y) - 1) : ((x) / (y)))
#ifndef __MIC_DEFINE__
#define __MIC_DEFINE__
static inline int64_t OBERON$MOD(int64_t x, int64_t y) { int64_t r = x % y; return r < 0 ? r + y : r; }
static inline int64_t OBERON$DIV(int64_t x, int64_t y) { int64_t q = x / y; int64_t r = x % y; return (r != 0 && ((x < 0) != (y < 0))) ? q - 1 : q; }
typedef struct MIC$AP { uint32_t $1; void* $; } MIC$AP;
typedef struct MIC$DA { uint32_t $1; void* $[]; } MIC$DA;
#endif
#define OP2$NoBreakPC (-1)
#define OP2$module 67
#define OP2$ident 40
#define OP2$period 18
extern void OP2$SignOn();
// no typedef for open array OP2$_$0 (uint8_t*)
extern void OP2$ParseOptions(MIC$AP name, MIC$AP options, MIC$AP path, MIC$AP pref, MIC$AP extension, int32_t* codeOpt, int32_t* parserOpt);
extern void OP2$WriteMsg(Texts$Reader source);
// no typedef for open array OP2$_$1 (uint8_t*)
extern void OP2$Module(Texts$Reader source, MIC$AP name, MIC$AP options, int32_t breakpc, uint8_t* error);
// no typedef for open array OP2$_$2 (uint8_t*)
/* public */ extern void OP2$CompileFile(MIC$AP name, MIC$AP opt, uint8_t* error);
extern void OP2$BEGIN$();
extern void OP2$init$(void);

#endif // __OP2_INCLUDED__