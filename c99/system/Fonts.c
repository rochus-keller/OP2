// Fonts.c
// this file was generated by ActiveOberon IDE 0.1.6 on Mo. März 9 20:11:23 2026

#include "Fonts.h"
#include <gc.h>
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <assert.h>
#include <math.h>
#include <ctype.h>
#include <stddef.h>

typedef struct $Class { struct $Class* super; } $Class;
static int $isinst(void* super, void* sub) {
    $Class* cls = ($Class*)sub;
    while(cls) {
        if( cls == super ) return 1;
        cls = cls->super;
    }
    return 0;
}
static MIC$DA* $toda(void** ptr) {
    return (MIC$DA*)((char*)ptr - offsetof(MIC$DA, $));
}
static void** $allocda(uint32_t count, int elemSize) {
    MIC$DA* ptr = (MIC$DA*)GC_MALLOC(sizeof(MIC$DA) + count * elemSize);
    if( ptr == 0 ) return 0;
    ptr->$1 = count;
    return ptr->$;
}

void Fonts$_$0$init$(Fonts$_$0* obj, unsigned int n) {
    int i;
    for( i = 0; i < n; i++ ) {
        obj[i].class$ = &Fonts$_$0$class$;
    }
}

struct Fonts$_$0$Class$ Fonts$_$0$class$ = { 
    0,
};

static int Fonts$initialized = 0;
void Fonts$init$(void) {
    if( Fonts$initialized ) return;
    Fonts$initialized = 1;
}

